# Simple FastAPI + OpenTelemetry + SigNoz
Write-Host "Starting FastAPI with OpenTelemetry..." -ForegroundColor Green

# Install dependencies
pip install -r requirements.txt

# Run the app
python app.py